
const noteReformatContent = (rows) =>{
    let final = ""
    // Object.keys(rows).forEach(e=>{
    //     if(e=="host"){

    //     }
    // })
    return final
}
export default noteReformatContent